# 可交付版清单（按实习六阶段）

> 项目根目录：`forest-fire-false-alarm`  
> 定稿测试：**38/38 = 100%**（`docs/验收材料/最终批量测试_20260724_152606.xlsx`）

---

## 总览目录

```text
forest-fire-false-alarm/
├── README.md                 # 启动与交付入口
├── requirements.txt
├── .env.example              # 多模态配置模板（勿提交真实 Key）
├── app/                      # 【阶段5】Web 原型与判别流水线
├── config/                   # 【阶段3/4】规则库 + few-shot
├── scripts/                  # 【阶段2/4/6】OpenCV / 多模态 / 批量评估脚本
├── tests/                    # 规则单元测试
├── data/
│   ├── samples/              # 【阶段1】分类样本（38）
│   ├── labels/               # 【阶段1】标注表（仅保留金标准）
│   ├── uploads/              # 运行时上传（空，可忽略）
│   └── results/              # 运行时可视化（空，可忽略）
└── docs/                     # 【阶段1/6】文档与验收材料
    ├── 00_交付清单.md         # 本文件
    ├── 01_误报分类表.md/.xlsx
    ├── 02_测试报告.md
    ├── 03_实习总结报告.md
    ├── 04_多模态Prompt说明.md
    └── 验收材料/
        ├── 样本标注表.xlsx/.csv
        ├── 最终批量测试_20260724_152606.xlsx
        └── 迭代节点/         # 测试报告对应关键节点（已去重）
```

---

## 阶段对照表

| 阶段 | 主要任务 | 交付位置 |
|------|----------|----------|
| **1. 业务理解与样本整理** | 误报分类、样本目录、标注表 | `docs/01_误报分类表.*`；`data/samples/`；`data/labels/样本标注表.*`；`docs/验收材料/样本标注表.*` |
| **2. 传统图像算法入门** | OpenCV 亮度/火焰/烟雾 | `app/opencv_analyzer.py`；`scripts/run_opencv_demo.py` |
| **3. 误报规则库建设** | 规则 JSON | `config/rules.json`；分类表文档 |
| **4. 多模态二次判别** | Prompt、接口、few-shot | `app/multimodal.py`；`config/few_shot.json`；`docs/04_多模态Prompt说明.md`；`scripts/test_qwen_vl.py` |
| **5. 原型系统开发** | 可演示 Web | `app/main.py` + `app/templates` + `app/static`；`app/pipeline.py` |
| **6. 测试总结** | 批量结果、报告 | `docs/02_测试报告.md`；`docs/03_实习总结报告.md`；`docs/验收材料/` |

---

## 测试报告关键节点（仅保留这些批量结果）

历史重复的 `批量测试_*.xlsx` 已删除；下列节点与 `02_测试报告.md` 第 4 节对应：

| 节点 | 文件 | 含义 | 严格准确率 |
|------|------|------|------------|
| N1 | `验收材料/迭代节点/N1_图片单独评估_20260722.xlsx` | 领导建议：图片单独评估（早期基线） | 10.0%（20 张） |
| N2 | `验收材料/迭代节点/N2_视频单独评估_20260722.xlsx` | 领导建议：视频单独评估（早期基线） | 11.1%（18 段） |
| N3 | `…/N3_多模态基线_抬真火偏高_准确率73.7pct_20260723.xlsx` | 多模态+偏防漏报，误报抬真火偏高 | 73.7%（漏报1 / 抬真火9） |
| N4 | `…/N4_压误报后_漏报回升_准确率84.2pct_20260723.xlsx` | 压抬真火后，漏报回升 | 84.2%（漏报6 / 抬真火0） |
| N5 | `…/N5_首次满分_准确率100pct_20260723.xlsx` | 融合修好后首次满分 | **100%** |
| **N6 定稿** | `验收材料/最终批量测试_20260724_152606.xlsx`（副本在迭代节点 N6） | **最终验收版** | **100%** |

---

## 演示与复现

```bash
cd ~/Desktop/forest-fire-false-alarm
source .venv/bin/activate
pip install -r requirements.txt
# 配置 .env（参考 .env.example）后：
uvicorn app.main:app --reload --host 127.0.0.1 --port 8000
```

批量复现定稿（耗额度，非必须）：

```bash
python scripts/eval_batch.py --media all
```

---

## 不纳入交付 / 已清理

- `data/labels` 下历史重复批量 Excel（已删）
- `data/uploads/*`、`data/results/*` 运行缓存（已清空，仅留 `.gitkeep`）
- `.env` 真实密钥（本地保留，勿打包外传；用 `.env.example`）
- `.venv/` 虚拟环境（接收方可自行创建）
